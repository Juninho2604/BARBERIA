# Variables públicas (visibles en el bundle del navegador): prefijo NEXT_PUBLIC_
# En Vercel se configuran desde el dashboard del proyecto.

# URL pública del API. En desarrollo apunta a localhost; en producción al VPS
# (Cloudflare Tunnel mientras no haya dominio).
NEXT_PUBLIC_API_URL=http://localhost:4000
